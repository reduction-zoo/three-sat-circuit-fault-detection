# Follow-up independent review of round 001

**Decision: advance to expert review.** Commit `e1f778a` repairs both findings in [review 001](../001/review.md). This is an eligible reconstruction of a known hardness mechanism under the fixed question, not a new complexity theorem or a publication judgment.

## Correctness

At [algorithm.py line 52](../../work/algorithm.py), the CLI removes Python's 4,300-digit integer conversion limit before JSON parsing or serialization. The previous [large-input reproducer](../001/check_large_input.py) now passes. My independent [repair check](check_repaired_cli.py) passes through construction and `--extract` in fresh processes for an unsatisfiable source whose variable count has 4,301 decimal digits. It checks the input count, seven gates, the designated output wire ID, and recovery of `NO-SOLUTION`. The new setting changes only JSON integer conversion; it does not alter the gate or decoder logic.

At [proof.md line 9](../../work/proof.md), the cost argument now bounds gates by the number `m` of clauses: at most `3m` cached negations, `2m` clause ORs, `m−1` conjunctions and one final AND, totaling at most `6m` for `m≥1`; the empty formula uses three gates. Since `m≤L=|x|` and every wire identifier has `O(log(n+m+2))=O(L)` digits, forward output length and bit time are polynomial in `L`, even when `n` is exponentially larger than its encoding length. Reconstruction in `G` has the same bound. For a valid complete target output, two explicit input vectors each have `n+1` bits, so copying the returned `n` bits is polynomial in `L+|y|`; for `NO-SOLUTION`, the fixed output is immediate. Large-integer conversion and arithmetic remain polynomial in their operand bit lengths. This supplies the missing premise and repairs the earlier false `O(n+m)` inference.

The unchanged circuit and extraction argument still holds on the actual current files: [algorithm.py lines 7–37](../../work/algorithm.py) produce a legal topological fan-in-two circuit whose sole designated output is `w=z∧φ(x)`. A stuck-at-1 fault there is always detected with `z=0`; stuck-at-0 is detected exactly when `φ` is satisfiable, with `z=1`. Thus both source answer classes yield a nonempty target answer set. For every valid complete target output, [lines 40–47](../../work/algorithm.py) find the sole stuck-at-0 entry and return a satisfying assignment, regardless of entry order or witness choice. For the valid target `NO-SOLUTION`, they return the source `NO-SOLUTION`. The maps are deterministic and call no solver. I found no remaining proof gap or counterexample in the repaired rule.

The prepared [check.py](../../work/check.py) and separate [verify.py](../../work/verify.py) are unchanged from commit `dda6eeb`, so I reuse the first review's inspection of their full injected-instance loops rather than rerunning their bounded suites. [verification.md](../../work/verification.md) reports 115 source instances and 201 independently solved target outputs in the prepared loop, plus 123 instances and 243 outputs in the separate loop, including unsatisfiable cases and alternate complete test sets. Those finite observations support the implementation but do not replace the general argument above.

## Novelty

I checked the primary source again on 2026-09-25: [Ibarra and Sahni, *Polynomially Complete Fault Detection Problems* (1975)](https://www.cise.ufl.edu/~sahni/papers/faultDetection.pdf), Section II problem P4 (printed p. 243), Theorem 3.4(1) and its proof (printed p. 248), and Theorem 3.5 Case 4 (printed p. 248). As established in review 001, Theorem 3.4 uses detectability of both output-line fault polarities as the nonconstancy test for a circuit function. The present `z∧φ` gating and complete search decoder adapt that known mechanism from the paper's 3-DNF tautology formulation to the fixed 3-CNF search and `NO-SOLUTION` contract. The [upstream issue](https://github.com/CodingThrust/problem-reductions/issues/919) also notes the direct formula-output stuck-at-0 connection. No claim of first use of this exact gating device or new hardness theorem is supported. Coverage beyond that paper and issue remains unresolved; it does not affect the fixed question, which expressly accepts a complete reconstruction.

## Significance and cost

The result supplies the required explicit construction and recovery for both fault polarities, including unsatisfiable inputs and every valid complete test set. It meets the fixed acceptance criterion as a reconstruction. Construction uses one extra input, at most `6m` gates (three for empty CNF), and one designated output. Numeric IDs cost `O(log(n+m))` bits each. A complete target answer is `Ω(n)` bits because it contains explicit input vectors; target solving may be exponential. No practical runtime gain was measured or claimed. The open reconstruction request is meaningful, while the underlying hardness mechanism is classical.

## Review boundary and route

This was a fresh registered `research-reviewer` follow-up context with the parent supplying the fixed paths and repair commit `e1f778a`. The model route was the inherited **GPT-6 Codex** route; no model override or more precise serving identifier was exposed. Agent-spawning tools were available, so no tool-denial mechanism or depth cap was in force. Filesystem permission was unrestricted (`danger-full-access`); the restriction to `reviews/002/` was enforced by instructions, not a sandbox. I spawned no agents and wrote only this review and its targeted check in `reviews/002/`. I did not edit candidate artifacts, original tests, or earlier evidence. These boundaries do not themselves establish any research judgment.
